input_data:
  version: # TF Data/Records object

build_params:
  # Model type options:
  # default, mobilenet_v2, efficientnet_b7, efficientnet_v2l
  # vgg16, vgg19, xception, resnet50
  model_type: efficientnet_v2l
  input_shape: !!python/tuple [224, 224, 3] # [rows, columns, channels]
  n_classes: 5
  n_filters: 32 
  dense_nodes: 256
  conv_dropout: 0.25 
  dense_dropout: 0.5  
  filter_size: !!python/tuple [3, 3] # [rows, columns]
  pool_size: !!python/tuple [2, 2] # [rows, columns]
  output_activation: linear

compile_params:
  loss: mean_squared_error
  optimizer: adam
  learning_rate: 0.001
  metrics: [rmse, mae]

train_params:
  batch_size: 32
  epochs: 200
  early_stopping: True
  patience: 10
  fine_tune: True

save_instructions:
  model_path:
  history_path: